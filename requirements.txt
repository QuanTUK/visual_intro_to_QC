# certain matplotlib features only work in python 3.13 or higher, make sure you have a recent version of python installed.

# Core Quantum Computing and Mathematical Libraries
qiskit>=2.3.0
qiskit-aer>=0.17.2          # Required for local quantum circuit simulation
numpy>=2.4.2               # Fundamental for state vector and matrix operations

# Interactive Development Environment
jupyter>=1.1.1
notebook>=7.5.3             # Or jupyterlab>=4.0.0, depending on preference
jupyterlab>=4.5.4

# Visualization and Interactive Widgets
ipywidgets>=8.1.8           # Critical for the interactive Dimensional Circle Notation (DCN) UI
matplotlib>=3.10.8           # Standard plotting backend for Qiskit
pylatexenc>=2.10            # Required by Qiskit for rendering high-quality circuit diagrams
hsluv>=5.0.4                # Enables perceptually uniform color spaces for DCN visualizations

# Our visualization package
qc_interactive_education_package>=0.2.0     # Note: If this is a local module rather than a PyPI package,
                            # it should remain commented out here. Ensure the module folder
                            # is in the project root or installed via `pip install -e .`